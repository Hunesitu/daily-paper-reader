---
title: "MEPA: Multi-Scale Representation Alignment for Visual Autoregressive Modeling with Mixture of Experts"
title_zh: MEPA：基于专家混合的多尺度表示对齐用于视觉自回归建模
authors: "Nuoyan Zhou, Zhijun Tu, Lei Yu, Kun Cheng, Jie Hu, Nannan Wang, Xinghao Chen"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00371v1"
tags: ["query:ce"]
score: 8.0
evidence: 面向图像生成的视觉自回归建模
tldr: 视觉自回归模型（VAR）在粗到细的多尺度生成中，低尺度捕获全局语义而高尺度关注细节，但共享架构导致优化冲突，且早期语义误差会传播。本文提出MEPA，通过尺度感知的token路由混合专家（MoE）架构实现解耦表示学习，并引入外部自监督特征进行残差聚合以增强早期语义。在ImageNet 256×256上，该模型以一半训练轮次和更小参数量达到更优FID，训练成本仅微增。结果表明，该方法显著提升了训练效率和生成质量，且优势随训练轮次增加而扩大。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00371v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1262, \"height\": 722, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00371v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1251, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00371v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1210, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00371v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1251, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00371v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1240, \"height\": 252, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00371v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1245, \"height\": 185, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 1555, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 618, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 603, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 612, \"height\": 215, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 582, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1250, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00371v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1193, \"height\": 224, \"label\": \"Table\"}]"
motivation: 解决VAR模型多尺度表示学习中共享架构的优化冲突及早期语义误差传播问题。
method: 提出尺度感知token路由MoE架构解耦多尺度学习，并结合外部自监督特征的残差聚合增强早期语义。
result: 在ImageNet上以一半训练轮次和更小参数预算获得更优FID，训练成本仅小幅增加。
conclusion: 显著提升VAR的训练效率和生成质量，且性能优势随训练轮次增加而扩大。
---

## 摘要
视觉自回归建模（VAR）开创了从粗到细的多尺度自回归生成范式，在图像生成中展现出强大能力。然而，VAR在多尺度表示学习方面仍存在固有缺陷。具体来说，较低尺度主要捕获全局语义，而较高尺度关注细粒度细节。跨尺度采用共享架构会导致优化冲突。此外，由于因果自回归过程，早期尺度的不准确语义可能传播并显著降低最终输出质量。为解决这些问题，我们引入了一种尺度感知的令牌路由专家混合（MoE）架构，允许尺度自适应的专家选择，从而促进跨尺度的解耦表示学习。此外，我们通过融合外部自监督特征来增强早期尺度的语义建模。与简单对齐不同，我们针对VAR范式分析并设计了一种残差特征聚合方案。大量实验表明，我们的方法显著提高了训练效率和生成质量。在ImageNet 256×256基准测试中，我们的模型在仅需默认训练周期一半和更小参数预算的情况下，相比密集基线取得了更优的FID，而训练成本仅略有增加。此外，随着训练周期的增加，性能差距进一步扩大。

## Abstract
Visual AutoRegressive modeling (VAR) has pioneered a coarse-to-fine multi-scale autoregressive generative paradigm, demonstrating strong capabilities in image generation. However, VAR still suffers from inherent deficiencies in multi-scale representation learning. Specifically, lower scales primarily capture global semantics, while higher scales focus on fine-grained details. Employing a shared architecture across scales induces optimization conflicts. Moreover, due to the causal autoregressive process, inaccurate semantics at early scales can propagate and significantly degrade the final output. To address these issues, we introduce a scale-aware token-routed Mixture of Experts (MoE) architecture, allowing scale-adaptive expert selection, thereby facilitating decoupled representation learning across scales. In addition, we enhance semantic modeling at early scales by incorporating external self-supervised features. Unlike naive alignment, we analyse and design a residual feature aggregation scheme tailored to the VAR paradigm. Extensive experiments show that our method significantly improves both training efficiency and generation quality. On the ImageNet 256*256 benchmark, our model achieves a superior FID compared to the dense baseline while requiring only half of the default training epochs and a smaller parameter budget, with a merely marginal increase in training cost. Moreover, the performance gap further widens with larger training epochs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：视觉自回归建模（VAR）通过粗到细的多尺度预测范式在图像生成中取得了优异性能。它利用下一个尺度预测，将图像分解为多尺度残差表示，并建立跨尺度的因果依赖。
- **核心问题**：
  1. **共享架构的优化冲突**：低尺度主要捕获全局语义，高尺度关注细粒度细节。所有尺度使用同一个Transformer架构，导致学习目标冲突，难以同时优化不同尺度的特征。
  2. **语义错误传播**：由于VAR的因果自回归过程，早期尺度（小尺度及中间尺度）的语义不准确会沿着生成链传播到后续大尺度，严重影响最终图像质量。
- **整体含义**：作者旨在解决VAR模型在多尺度表示学习中的固有缺陷，提升训练效率和生成质量。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 引入**尺度感知的令牌路由混合专家（Scale-aware Token-routed MoE，STMoE）**，通过自适应专家选择实现跨尺度解耦表示学习。
  - 引入**语义引导（Semantic Guidance，SG）**，利用外部自监督视觉特征（如DINOv3）增强早期尺度的语义准确性，减少错误传播。

- **关键技术细节**：
  1. **STMoE 架构**：
     - 在Transformer块中，将原始的FFN替换为MoE层，包含多个专家（FFN子网络）。
     - **路由策略**：将尺度嵌入（scale embedding）直接添加到令牌特征中，使令牌感知自身所属尺度，然后通过令牌级路由器计算令牌到专家的亲和度分数，选择Top-k专家处理每个令牌。
     - 相较于简单的尺度级路由（SMoE），STMoE避免了同尺度内路由同质化，并改善了专家负载均衡。
     - 使用负载均衡损失函数 \( \mathcal{L}_b \) 确保各专家被均衡激活。
  2. **残差特征聚合与语义引导**：
     - **特征变换**：对VAR模型中间层的输出，按尺度升序排序，通过上采样和累积求和得到一组逐步丰富的聚合特征（从尺度1到当前尺度的累加结果）。
     - **语义对齐**：仅对早期尺度（如尺度1-5、1-6、1-7）的聚合特征与预训练视觉编码器（DINOv3）的特征进行余弦相似度最大化（见公式12），损失函数 \( \mathcal{L}_{SG} \)。
     - 最终训练损失：\( \mathcal{L} = \mathcal{L}_{SG} + \lambda \mathcal{L}_{CE} \)，其中 \( \lambda = 0.5 \)。

- **公式或算法流程（文字说明）**：
  1. 输入图像经过VAR tokenizer得到多尺度残差令牌序列 \( r_1, \ldots, r_K \)。
  2. 每个VAR块（部分块）使用STMoE层：计算令牌特征与尺度嵌入之和，经过路由器得到专家选择，输出加权融合结果。
  3. 在训练过程中，取中间层输出，按尺度累积求和得到聚合特征 \( z_j \)。
  4. 选择前几个尺度的聚合特征与DINOv3编码的图像特征计算 \( \mathcal{L}_{SG} \)。
  5. 联合 \( \mathcal{L}_{SG} \) 和原始交叉熵损失 \( \mathcal{L}_{CE} \) 优化模型。

## 3. 实验设计：数据集/场景、基准测试、对比方法

- **数据集**：ImageNet-1K，分辨率 256×256（主要实验），同时报告了 512×512 的初步结果。
- **基准测试**：类条件图像生成，评估指标：FID↓、IS↑、Precision、Recall、sFID。
- **对比方法**：
  - GANs：BigGAN, GigaGAN, StyleGAN-XL
  - 扩散模型：ADM, CDM, LDM, DiT
  - 自回归模型（非VAR）：VQGAN, RQTran, RCG, MaskGIT, MAR, FAR, LlamaGen, PAR
  - VAR系列：VAR-d16, VAR-d20, FlexVAR, SpectralAR
  - 对比方法中还特别与REPA（扩散模型的表示对齐方法）进行了比较。

## 4. 资源与算力

- 论文未明确说明所使用的GPU型号、具体数量及总训练时长。
- 但报告中提到：
  - 训练VAR-d12和d16模型在ImageNet上，batch size=96，使用AdamW优化器。
  - 每epoch训练时间约1h56min（针对MEPA-d16，见表7）。
  - 训练200 epochs为完整配置。
- 总体来看，作者未提供详细的算力硬件描述，仅给出了时间效率的粗略数据。

## 5. 实验数量与充分性

- **实验数量**：较为充分。
  - 主实验结果（表2）：对比多种方法在256×256下的FID/IS等，包含不同epoch（100,200）。
  - 消融实验（表3）：STMoE和SG各自贡献。
  - MoE类型对比（表4）：包括MoEEC、MoE++、SMoE、STMoE。
  - 不同自监督编码器对比（表5）：Mocov3、DINOv2、DINOv3。
  - 不同聚合策略对比（表6）：Last、L（最大3个）、S（最小3个）、M（中间3个）。
  - 训练过程中FID/IS/Precision曲线（图5）：有无语义引导的对比。
  - 512×512初步结果（表8）。
  - 与REPA的比较（表7）。
  - 路由热力图可视化（图4）。
- **充分性与公平性**：
  - 消融实验设计合理，逐步验证各组件效果。
  - 对比方法均为代表性工作，且报告了参数、步数、epoch等关键信息。
  - 但存在一定局限性：未与最新的大规模VAR变体（如VAR-d24/d30）对比；512×512任务只训练66 epoch，未完全收敛，结论不够严谨。
  - 整体上，实验覆盖了核心问题，但可进一步扩展更多尺度和更大模型。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. STMoE能够有效解耦多尺度表示学习，通过令牌级自适应路由提升生成质量，且负载均衡优于SMoE。
  2. 语义引导（SG）通过约束早期尺度语义，显著加速训练收敛（最高2×速度提升），并提升最终生成质量。
  3. 联合STMoE和SG，MEPA在ImageNet 256×256上以一半训练epoch（100 epochs）即达到或超越VAR-d16/d20在200 epochs的性能，FID分别降低至2.65（vs VAR-d16 4.10）和2.32（vs VAR-d16 3.55，200 epochs）。
  4. MEPA的改进效果随训练epoch增加而扩大，说明其不仅加速收敛，还提升了上限。
- **发现**：残差特征需要累积聚合后才能与自监督特征有效对齐，直接对齐单尺度残差效果不佳；中间尺度的聚合特征最为适合。

## 7. 优点

- **方法创新性**：首次系统分析VAR多尺度表示学习问题，并针对性地提出STMoE和残差聚合语义引导。
- **效率显著**：在减少训练成本的同时提升质量，具有实际部署价值。
- **分析深入**：通过路由热力图、负载均衡方差等分析，清晰展示了STMoE的优势。
- **实验设计全面**：覆盖了多种消融、对比、可视化，验证了各组件有效性。
- **与相关工作的明确差异**：指出MEPA不同于REPA的直接扩展，而是针对VAR特性专门设计，并在表7中公平比较。

## 8. 不足与局限

- **硬件资源未明确**：缺乏GPU型号、数量等细节，影响复现和能耗评估。
- **实验覆盖范围有限**：
  - 只在ImageNet上实验，未在其他数据集（如FFHQ、LSUN）验证泛化性。
  - 模型规模仅扩展到d16和d20，未测试更大模型（如d24、d30等）。
  - 512×512实验只训练66 epoch，未充分收敛，结论说服力不足。
- **计算开销**：MEPA在推理时间上增加了0.85s（对比VAR的0.50s），训练每epoch增加约4.5%时间，虽不大但仍存在。
- **对自监督编码器的依赖**：需要额外预训练模型（DINOv3），增加了流程复杂性。
- **未讨论无条件生成或文本条件生成**：仅针对类条件生成。

（完）
