---
title: Balancing Performance and Diversity in GRPO Autoregressive Text-to-Image Post-Training
title_zh: 在GRPO自回归文生图后训练中平衡性能与多样性
authors: "Yuanhao Chiang, Hongbo Duan, Chunru Yang, Jiahua Pei, Yi Liu, Xueqian Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21498v1"
tags: ["query:ce"]
score: 8.0
evidence: 自回归文本到图像生成的后训练优化
tldr: 自回归文生图模型对齐面临挑战，现有GRPO方法固定参考策略散度。本文在统一f-散度框架下研究前向KL、反向KL和JS散度，发现JS正则化在采样token塑造下可缓解均匀偏差并阻止大偏离。在LlamaGen和Janus-7B上，JS散度在多数指标上达到最优或高度竞争性能，同时保持生成多样性。该工作揭示了散度选择对性能与多样性的影响，为在线RL文生图对齐提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21498v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1281, \"height\": 1047, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21498v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 1051, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21498v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1172, \"height\": 701, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21498v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21498v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 601, \"label\": \"Table\"}]"
motivation: 现有GRPO文生图对齐固定参考策略散度，忽略其对策略优化的影响，导致性能与多样性失衡。
method: 提出统一f-散度框架，分析前向KL、反向KL和JS散度，采用采样token塑造的JS正则化实现权衡。
result: 在LlamaGen和Janus-7B上，JS散度在多数评价指标上取得最强或高度竞争性能，并保持良好生成多样性。
conclusion: 所提方法有效平衡了性能与多样性，为自回归文生图对齐提供了新的正则化策略。
---

## 摘要
自回归文生图（T2I）生成近期取得了快速进展，但将生成图像与人类偏好对齐仍然具有挑战性。GRPO风格的在线强化学习提供了一个有效框架；然而，现有方法通常将参考策略散度视为固定值，尽管它直接影响策略优化。我们在统一的f-散度框架（包括前向KL、反向KL和JS散度）中研究了这一被忽视的因素，用于GRPO风格的自回归T2I对齐。我们的系统理论分析揭示了不同散度以不同方式重塑令牌级更新。特别地，在使用采样令牌塑造形式下，JS正则化通过减轻相对于参考策略的均匀偏差，同时仍然阻止大偏差，实现了有利的权衡。在LlamaGen和Janus-7B上的大量实验表明，JS散度在大多数评估指标上实现了最强或高度竞争的优化性能，同时保持了良好的生成多样性。代码可在https://github.com/tuoyou-hao/BPD-GRPO获取。

## Abstract
Autoregressive text-to-image (T2I) generation has recently advanced rapidly, yet aligning generated images with human preferences remains challenging. GRPO-style online reinforcement learning provides an effective framework; however, existing methods typically treat reference-policy divergence as fixed, despite its direct impact on policy optimization. We study this overlooked factor within a unified f-divergence framework, encompassing forward KL, reverse KL, and JS divergence, for GRPO-style autoregressive T2I alignment. Our systematic theoretical analysis reveals that different divergences reshape token-level updates in distinct ways. In particular, under the sampled-token shaping form used, JS regularization achieves a favorable trade-off by mitigating uniform bias relative to the reference policy while still discouraging large deviations. Extensive experiments on LlamaGen and Janus-7B show that JS divergence achieves the strongest or highly competitive optimization performance on most evaluation metrics while maintaining favorable generation diversity. The code is available at https://github.com/tuoyou-hao/BPD-GRPO.

---

## 论文详细总结（自动生成）

# 论文总结：Balancing Performance and Diversity in GRPO Autoregressive Text-to-Image Post-Training

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：自回归文生图（T2I）生成近年来发展迅速，但生成的图像与人类偏好对齐仍是挑战。GRPO 风格的在线强化学习为对齐提供了有效框架，但现有方法通常将参考策略散度视为固定的设计选择（默认使用 KL 惩罚），忽视了散度形式对策略优化的直接影响。
- **核心问题**：不同散度（前向 KL、反向 KL、JS 散度）在 GRPO 式自回归 T2I 对齐中如何影响令牌级优化动态？能否通过选择更合适的散度来平衡对齐性能与生成多样性？
- **整体含义**：论文强调散度选择不仅是正则化细节，而是从根本上塑造了优化动态的关键因素。JS 散度在采样令牌塑造形式下能够提供更平衡的梯度调节，从而在保持高性能的同时维持多样性。

## 2. 论文提出的方法论
### 核心思想
- 将 GRPO 式在线强化学习中的参考策略散度纳入统一 **f-散度框架**，系统比较 Reverse KL、Forward KL 和 JS 散度的梯度塑造效果。
- 在采样令牌（sampled-token）梯度塑造形式下，推导出合并优势函数：  
  `Â_t = A_t - η·f'(w_t)`，其中 `w_t = π_θ / π_ref` 是令牌级重要性比率。
- 不同散度的 f' 函数不同：  
  - RKL: f' = log w + 1（对 w>1 时产生强抑制）  
  - FKL: f' = -1/w（始终为负，起到放大更新作用）  
  - JS: f' = log(2w/(1+w))（在 w=1 时为 0，对 w>1 抑制、w<1 放大，且无恒定偏置）
- **关键发现**：JS 散度在参考点附近无偏移，且能根据 w 大小自适应调节梯度，从而在避免均匀偏差的同时防止过激更新，实现性能与多样性的权衡。

### 关键技术细节
- 延续 GRPO 框架：对每个提示采样 G 个图像令牌序列，计算组内相对优势，并广播到每个有效图像令牌。
- 目标函数：  
  `J(θ) = E[R(x,y)] - η·Σ_t D_f(π_θ(·|x,y_<t) || π_ref(·|x,y_<t))`
- 梯度推导显示散度仅通过 f'(w) 影响合并优势，其他组件（采样、优势计算、优化计划）在不同散度间保持一致。
- 实际 JS 计算：使用对数比率 Δ_t = log π_θ - log π_ref，并计算 `f'_JS = log 2 + Δ_t - log(1+exp(Δ_t))`，确保数值稳定。

## 3. 实验设计
### 数据集 / 场景
- **性能评估数据集**：DPG-Bench、GenAI-Bench、GenEval。
- **多样性评估数据集**：同上三个基准，但使用额外的多样性指标。
- **Benchmark 方法**：仅对比不同散度变体（Reverse KL、Forward KL、JS 散度），无其他外部基线（但统一使用 GRPO 框架）。

### 对比方法
- 论文未与最新文生图后训练方法（如 DPO、SPO 等）对比，而是聚焦于 GRPO 框架内散度选择的消融对比。这保证了实验公平性，但缺少外部 SOTA 比较。

### 评估指标
- **性能指标**：CLIPScore、HPS-v2.1、ImageReward、VQAScore（越高越好）。
- **多样性指标**：1D/2D 熵、FSIM、I-I CLIP（越低越好）、LPIPS、RMSE、PSNR、SSIM（综合反映感知和结构多样性）。

## 4. 资源与算力
- **GPU**：4 块 NVIDIA A100 80GB GPU。
- **训练步数**：  
  - Janus-7B：1600 步，学习率 1e-6，batch size 2，线性学习率调度。  
  - LlamaGen：900 步，学习率 1e-5，batch size 32，余弦学习率调度。
- 论文未报告总训练时间、参数规模等细节，但算力信息明确。

## 5. 实验数量与充分性
- **实验数量**：两种模型（LlamaGen、Janus-7B）× 三种散度变体（RKL、FKL、JS）× 三个评估基准，共 18 组性能评测 + 18 组多样性评测。此外提供了训练奖励动态图（图3）和定性对比图（图1、图2）。
- **充分性**：
  - 覆盖了两种主流自回归 T2I 模型（LlamaGen 基于 Llama 架构，Janus-7B 多模态），验证方法的通用性。
  - 性能指标和多样性指标全面，分别从语义对齐、人类偏好、感知质量、分布宽度、结构差异等多角度评估。
  - 缺乏超参数（如 η）的敏感性分析或更广泛散度（如 α-divergence）的比较。
  - 实验设计公平：保持采样策略、优势计算、优化计划完全一致，仅改变散度公式。
- **客观性**：结果报告全面，未选择性展示有利结果。JS 在多数指标上领先或持平，但某些多样性指标（如 FSIM、I-I CLIP）上 RKL 或 FKL 仍可能更好，论文如实报告并说明 JS 实现了平衡。

## 6. 论文的主要结论与发现
- JS 散度正则化在 GRPO 式自回归 T2I 对齐中取得了最佳的性能-多样性权衡。在 4 个性能指标上，JS 在 Janus-7B 上获得 3 个最佳结果（HPS、ImageReward、VQAScore），在 LlamaGen 上获得 3 个最佳（CLIPScore、ImageReward、VQAScore）。
- 多样性方面，JS 在 12 个熵指标中 7 个最高，且未在任何单一差异指标上走向极端（如 RKL 导致过高感知差异，FKL 导致熵崩溃），保持了视觉分布覆盖和空间复杂度。
- 理论分析验证：JS 的 f' 在参考点处为 0，抑制过度更新同时鼓励探索，而 RKL 导致恒定抑制，FKL 导致放大，均不利于平衡。
- 训练动态显示 JS 变体在后期保持轻微奖励优势，RKL 保守，FKL 不足。

## 7. 优点
- **理论深度**：通过 f-散度框架统一分析不同散度的梯度塑造效果，利用不等式证明 JS 的 f' 介于 FKL 和 RKL 之间，提供清晰直观的解释。
- **实验严谨**：严格控制变量（仅改变散度公式），确保比较公正。同时覆盖性能与多样性双方面，避免单一指标误导。
- **实用价值**：提出的 JS 正则化无需改变 GRPO 整体流程，仅需替换 f' 计算，易于集成到现有管线。
- **代码开源**：提供 Github 仓库，可复现结果。

## 8. 不足与局限
- **外部对比缺失**：仅对比散度变体，未与 DPO、SPO 等非 GRPO 方法或其他后训练方式（如 GCPO）进行性能对比，无法判定 JS+GRPO 在整体文生图后训领域的绝对优势。
- **超参数依赖**：所有变体使用相同的 η（未报告具体值），但散度改变可能最佳 η 不同，缺乏 η 消融实验。
- **奖励模型单一**：仅使用 HPS-v2.1 作为在线奖励模型，未验证其他奖励模型（如 ImageReward 自身、Aesthetic Score 等）下结论是否一致。
- **模型局限性**：实验限于 LlamaGen 和 Janus-7B，未扩展至更大规模模型（如 Janus-Pro 或更高分辨率生成器），泛化性需进一步验证。
- **计算资源有限**：仅使用 4 块 A100，训练步数较短（900-1600 步），长期训练下差异可能变化。未报告训练时间。
- **多样性指标解释力**：部分多样性指标（如 FSIM、SSIM）对均匀颜色区域敏感，可能不完美反映生成分布的真实宽度。论文已使用多维度指标，但仍存在量化偏差风险。

（完）
