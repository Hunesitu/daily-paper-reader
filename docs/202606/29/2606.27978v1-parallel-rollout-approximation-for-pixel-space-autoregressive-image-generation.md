---
title: Parallel Rollout Approximation for Pixel-Space Autoregressive Image Generation
title_zh: 像素空间自回归图像生成的并行 rollout 近似方法
authors: "Jiayi Xu, Di He, Guolin Ke"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27978v1"
tags: ["query:ce"]
score: 9.0
evidence: 自回归图像生成模型 - 像素空间自回归生成方法
tldr: 像素空间自回归图像生成面临高维补丁生成误差大和训练-推理不一致的问题。提出并行滚动近似(PRA)，先生成低维中间状态再解码为像素，并在训练时并行构建推理样式的像素输入。PRA-S在ImageNet-1K上以135M参数达到FID 2.58，超越先前十亿参数模型；PRA-L以511M参数达到FID 1.94，创像素空间AR新纪录。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27978v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1189, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27978v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1412, \"height\": 1064, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27978v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1096, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 793, \"height\": 1266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1050, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 570, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 575, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 397, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27978v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1421, \"height\": 261, \"label\": \"Table\"}]"
motivation: 像素空间自回归模型因高维补丁产生大误差，且教师强制训练导致推理时误差累积，现有缓解方法效果有限。
method: 提出并行滚动近似，先生成低维中间状态再解码为像素，并在训练时通过相同路径并行构造推理风格的像素输入。
result: PRA-S以135M参数在ImageNet-1K上FID达2.58，超越先前十亿参数结果；PRA-L以511M参数FID达1.94，创像素空间AR新纪录。
conclusion: PRA有效解决像素空间AR的两大难题，在生成质量上达到新高度，并提升分类探针准确率，具备统一生成与理解的潜力。
---

## 摘要
像素空间连续标记自回归（AR）生成直接对原始像素块序列进行图像建模，避免了离散标记化或单独预训练的标记器。然而，它面临双重挑战：高维像素块生成导致单步误差大，以及教师强制训练造成训练-推理差距，使得这些误差在AR步骤中累积。现有的修复方法如$x$-预测和输入噪声注入仅能部分缓解这些问题。精确的rollout训练能更好地匹配推理时的条件，但由于顺序采样速度过慢而不切实际。我们提出了一种可扩展的框架——并行rollout近似（PRA），它联合解决这两个挑战。PRA首先生成低维中间状态，而不是高维像素块，然后通过像素解码器将其映射回像素空间标记，从而保留像素输入、像素输出的AR接口。它还在推理时通过相同的中间状态到像素路径独立地构造类似推理的像素输入，近似推理时rollout遇到的像素反馈接口，同时保持并行的教师强制训练。在类别条件ImageNet-1K $256\times256$分辨率生成任务上，具有135M参数的PRA-S实现了2.58的FID，超过了之前十亿级像素空间AR结果（3.60）。将PRA扩展到511M参数的PRA-L进一步将FID提升至1.94，在像素空间AR模型中建立了新的最优水平。除了生成任务外，PRA在ImageNet分类探测准确率上优于其他AR和扩散基线，表明其在统一的像素空间图像生成与理解中的潜力。

## Abstract
Pixel-space continuous-token autoregressive (AR) generation directly models images as sequences of raw pixel patches, avoiding discrete tokenization or a separately pretrained tokenizer. However, it faces coupled challenges: high-dimensional patch generation causes large single-step errors, and teacher-forced training creates a train--inference gap that makes these errors accumulate across AR steps. Existing fixes such as $x$-prediction and input noise injection only partially mitigate these issues. Exact rollout training better matches inference-time conditions, but is impractical due to prohibitively slow sequential sampling. We propose \emph{Parallel Rollout Approximation} (PRA), a scalable framework that addresses both challenges jointly. PRA generates low-dimensional intermediate states instead of high-dimensional pixel patches, then maps them back to pixel-space tokens with a pixel decoder, preserving a pixel-in, pixel-out AR interface. It also constructs inference-like pixel inputs through the same intermediate-state-to-pixel path used at inference, independently across positions, approximating the pixel-feedback interface encountered during inference-time rollout while retaining parallel teacher-forced training. On class-conditional ImageNet-1K generation at $256\times256$ resolution, PRA-S with 135M parameters achieves an FID of 2.58, surpassing the previous billion-scale pixel-space AR result of 3.60. Scaling to PRA-L with 511M parameters further improves FID to 1.94, establishing a new state of the art among pixel-space AR models. Beyond generation, PRA achieves higher ImageNet classification probing accuracy than other AR and diffusion baselines, suggesting its potential for unified pixel-space image generation and understanding.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：像素空间自回归（AR）图像生成面临两大耦合挑战：
  - **输出端**：直接预测高维像素块（如768维）导致单步误差大；
  - **输入端**：教师强制训练使用真实前缀，而推理时使用自生成前缀，造成训练-推理不匹配，误差累积。
- **现有方法不足**：$x$-预测和输入噪声注入只能部分缓解，精确的rollout训练因顺序采样代价过高而无法实用。
- **研究意义**：像素空间AR可避免离散标记化或外部预训练标记器，实现端到端原始像素建模，有望统一生成与视觉理解。但此前性能远落后于扩散模型。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出**并行rollout近似（PRA）**，联合解决输出端和输入端问题：
  - **输出端**：生成低维中间状态（如16维）而非高维像素块，再通过像素解码器映射回像素空间，降低单步生成难度。
  - **输入端**：在训练时，通过相同的中间状态→像素路径，独立并行地构造“推理风格”的像素输入（使用扰动后的中间状态经解码得到），避免顺序rollout，同时保留像素输入/输出接口。
- **关键技术细节**：
  - **中间状态构造**：对每个像素块 $x_i$，结合因果前缀表示 $h_{i-1}$ 通过轻量投影网络 $g_\psi$ 得到低维目标 $z_i = g_\psi(x_i, h_{i-1})$。使用token掩码（遮蔽当前像素块）鼓励依赖上下文。
  - **像素解码器 $D_\phi$**：因果Transformer，将中间状态序列解码为像素块。
  - **训练过程**（算法1）：
    1. 教师强制前向通过真实像素序列得到 $h_{i-1}$，构造 $z_i$；
    2. 对 $z_i$ 加扰得到 $\tilde{z}_i^t$（插值噪声），解码得到 $\bar{x}_i$；
    3. 第二次并行前向：以 $\bar{x}_{<i}$ 为输入（停止梯度），训练扩散头预测 $z_i$。
  - **推理过程**（算法2）：顺序采样：生成 $\hat{z}_i$ → 解码得 $\hat{x}_i$ → 反馈作为下一步输入。
- **训练目标**：结合整流流损失、重建损失（$\ell_1$ + LPIPS）和辅助表示损失。

#### 3. 实验设计
- **数据集**：ImageNet-1K，类条件生成，分辨率 $256\times256$。
- **评估指标**：FID（主要）、IS，使用ADM评估套件，50K生成样本 vs. ImageNet参考统计。
- **对比方法**：
  - **两阶段扩散**：DiT-XL/2, SiT-XL/2, REPA-XL/2, LightningDiT-XL/2, DDT-XL/2, RAE-XL/2
  - **两阶段自回归**：VAR-d20, LlamaGen-XL, MAR-B/L, SphereAR-L
  - **像素空间扩散**：ADM-G, SiD, PixelFlow, PixNerd, JiT-B/L
  - **像素空间自回归**：JetFormer, FractalMAR-B/L/H, FARMER-1.1B/8, FARMER-1.9B/8

#### 4. 资源与算力
- 论文明确报告了训练成本（附录表7）：
  - PRA-S（135M）：在8×A100 GPU上训练400 epoch需3.125天；
  - PRA-B（250M）：6天；
  - PRA-L（511M）：14.3天。
- 批量大小512，使用AdamW优化器，峰值学习率3e-4。

#### 5. 实验数量与充分性
- **主要实验**：对比了三个模型规模（S/B/L）与十余种基线，报告了FID和IS。
- **消融实验**（100 epoch训练，PRA-B规模）：
  - **输出端消融**：测试了目标构造方式（LDM编码器、局部仅编码、前缀感知）、掩码概率（0.0/0.3/0.5/0.7）、中间状态维度（8/16/32/64）；
  - **输入端消融**：测试了不同训练输入（真实像素、加噪像素、真实中间状态、加噪中间状态、解码像素），以及扰动强度（tmin = 0.0~1.0）。
  - **线性探测实验**：在ImageNet-1K上训练线性分类器，比较PRA-L与DiT/SphereAR/JiT的top-1准确率。
- **充分性**：实验覆盖了主要挑战的各个维度，对比公平（相同分辨率、评估协议），消融充分揭示了各组件的贡献。客观性较好。

#### 6. 主要结论与发现
- PRA-S（135M）FID 2.58，超越之前最大的像素空间AR模型FARMER-1.9B（FID 3.60）；PRA-L（511M）FID 1.94，创像素空间AR新SOTA。
- PRA在模型规模增长时性能持续提升，在像素空间AR方法中拉开了与第二名（3.60→1.94）的显著差距。
- 与两阶段方法和像素空间扩散方法相比，PRA仍具竞争力，且直接建模原始像素。
- 线性探测：PRA-L top-1准确率68.80%，大幅高于SphereAR-L（52.19%）和JiT-L（42.76%），表明像素空间AR学习到的表示更有利于视觉理解。

#### 7. 优点
- **方法创新**：联合解决输出端和输入端问题，通过低维中间状态降低生成难度，并并行构造推理风格输入，避免rollout代价。
- **端到端学习**：无需预训练标记器或外部自编码器。
- **可扩展性好**：三个规模均取得优秀结果，训练成本相对合理。
- **双重能力**：生成质量领先的同时，分类探测性能也显著优于同类方法，体现统一模型的潜力。
- **充分的消融实验**：清晰验证了两大核心设计的互补贡献。

#### 8. 不足与局限
- **计算开销**：虽然避免了顺序rollout，但训练仍需两次并行前向（一次用于构造目标/解码，一次用于AR训练），并引入像素解码器，训练成本高于标准AR（但仍可接受）。
- **领域验证**：仅测试了ImageNet-1K类条件生成，未在更大规模、更多样化的数据（如文本条件、高分辨率、视频）上验证，泛化性待考。
- **架构复杂性**：引入了中间状态编码器、像素解码器、扩散头等多个额外模块，整体框架比简单AR更复杂。
- **扰动策略依赖调参**：$t_{\min}$、掩码概率、梯度缩放等超参数需要手动调整，可能影响可复现性。
- **实验对比的局限性**：与一些近期SOTA（如REPA、DDT、RAE）相比仍有一定差距，但那些方法依赖外部标记器或更大模型，适用场景不同。

（完）
