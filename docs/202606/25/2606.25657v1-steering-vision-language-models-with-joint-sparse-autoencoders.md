---
title: Steering Vision-Language Models with Joint Sparse Autoencoders
title_zh: 联合稀疏自编码器引导视觉-语言模型
authors: "Huizhen Shu, Xuying Li, Hongxu Lin, Wenjie Sun, Hui Li"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25657v1"
tags: ["query:ce"]
score: 8.0
evidence: 通过联合稀疏自编码器在视觉语言模型中实现概念抑制
tldr: 针对现有稀疏自编码器在视觉-语言模型中难以获得可控跨模态特征的问题，本文提出联合稀疏自编码器(JSAE)，通过显式对齐约束联合分解视觉和语言激活，得到共享的可解释特征。在LLaVA上，JSAE恢复了食物、动物等概念，双向干预表明加性干预在中后期层最强，抑制效果在各层相近。在三个VLM上验证了层局部效应，表明显式对齐的稀疏表示支持更可控的多模态特征干预分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25657v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1654, \"height\": 1109, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 782, \"height\": 577, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1636, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1482, \"height\": 1428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 508, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 508, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25657v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 508, \"height\": 508, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25657v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1645, \"height\": 893, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25657v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 1699, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25657v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1647, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25657v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1180, \"height\": 774, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25657v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1104, \"height\": 684, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25657v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1589, \"height\": 315, \"label\": \"Table\"}]"
motivation: 现有SAE用于VLM难以得到可控的跨模态方向。
method: 提出JSAE，用显式对齐约束联合分解序列池化的视觉和语言激活。
result: JSAE恢复跨模态概念，加性干预在中间层最强，抑制在各层相近。
conclusion: 显式对齐的稀疏表示比无约束方法更支持可控干预分析。
---

## 摘要
稀疏自编码器在分析语言模型方面展现出潜力，但将其应用于视觉-语言模型时，通常得到的表示难以用作可控的跨模态引导方向。我们提出了联合稀疏自编码器，它利用显式对齐约束，将序列池化的视觉和语言激活联合分解为共享的、可解释的图像/字幕级特征。应用于LLaVA时，联合稀疏自编码器恢复了可识别概念（如食物和动物）的跨模态特征。通过双向干预（加法引导和抑制），我们在我们的协议下观察到一种层依赖的不对称性：加法引导在中到后期（输出前）层达到峰值，在两端减弱，而抑制得分在所有探测的层中均保持在统计噪声内的可比范围。在三个VLM上，即LLaVA-v1.6-Mistral-7B、Llama3-LLaVA-8B和基于MoE的Qwen3-VL-30B，实验显示不同架构具有相关的层局部化效应。总之，这些结果表明，与这里测试的无约束替代方法相比，显式对齐的稀疏表示支持在可识别的层范围内对多模态特征进行更可控的基于干预的分析。

## Abstract
Sparse Autoencoders (SAEs) have shown promise for analyzing language models, but applying them to vision-language models (VLMs) often yields representations that are difficult to use as controllable cross-modal steering directions. We introduce the Joint Sparse Autoencoder (JSAE), which uses an explicit alignment constraint to jointly factorize sequence-pooled vision and language activations into shared, interpretable image/caption-level features. Applied to LLaVA, JSAE recovers cross-modal features for recognizable concepts (e.g., food and animals). Through bidirectional interventions (additive steering and suppression), we observe a layer-dependent asymmetry under our protocol: additive steering peaks at mid-to-late (pre-output) layers and weakens at both ends, whereas suppression scores remain within a comparable range across all probed layers within statistical noise. Experiments on three VLMs, namely LLaVA-v1.6-Mistral-7B, Llama3-LLaVA-8B, and the MoE-based Qwen3-VL-30B, show related layer-localized effects across architectures. Together, these results suggest that explicitly aligned sparse representations support more controllable intervention-based analysis of multimodal features, within an identifiable layer range, than the unconstrained alternatives tested here.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的稀疏自编码器在语言模型上表现良好，但应用于视觉-语言模型时，得到的稀疏表示往往难以直接作为可控的跨模态引导方向——即无法通过简单的激活加法来可靠地控制生成内容的语义。
- **背景**：视觉-语言模型通过多模态对齐实现图像理解与语言生成，但其内部表示如何组织、对齐在何层出现、是否可干预等问题尚不清楚。已有SAE扩展（如VL-SAE）采用共享字典或基于共现统计，但缺乏显式的跨模态方向对齐，因此不能直接提供可引导的跨模态向量。
- **整体含义**：本文旨在提供一种可解释、可干预的稀疏分解框架，帮助理解VLM内部的对齐机制，并实现可控的多模态生成。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过联合稀疏自编码器，将平均池化后的图像级和文本级激活同时分解为稀疏的、共享语义的、方向对齐的特征，使得文本侧解码器列可以作为跨模态引导向量。
- **关键技术细节**：
  - **序列池化**：对目标层的隐藏状态按模态（视觉token、文本token）分别进行平均池化，得到宏观表示 \( A_v, A_t \in \mathbb{R}^d \)。
  - **JSAE结构**：分别使用视觉编码器-解码器对和文本编码器-解码器对，将池化表示映射到共享过完备潜在空间 \( \mathbb{R}^k \)（\( k \gg d \)）。激活函数为ReLU，得到非负稀疏编码 \( z_v, z_t \)。
  - **损失函数**：由三项组成：
    - 重建损失 \( L_{recon} \)：均方误差，保留原始信息；
    - 稀疏损失 \( L_{sparse} \)：L1范数，鼓励稀疏性；
    - 对齐损失 \( L_{align} \)：余弦相似度约束，对配对编码 \( z_v, z_t \) 施加方向对齐。
    - 超参数：\( \lambda_r = 1.0, \lambda_s = 0.03, \lambda_a = 1.0 \)。
  - **设计理由**：余弦对齐偏置使得编码器倾向于选择相同子集维度，解码器列对 \( w_v^{(i)}, w_t^{(i)} \) 编码同一语义因子，且文本侧列 \( w_t^{(i)} \) 近似位于语言模型线性解嵌入层可读的子空间，因此可作为引导向量注入图像token位置。
  - **直接语义特征发现**：通过方差过滤、L2归一化激活轮廓、K-Means聚类和Silhouette评价，将原子特征聚合成宏观语义簇，用于概念级引导。
  - **因果干预**：
    - 加法引导：将归一化的文本解码器列按概念聚合后，乘以强度系数 \( \alpha \)，累加到图像token位置的隐藏状态上。
    - 抑制干预：在负方向施加同样的向量（相减）。
- **算法流程**（文字说明）：
  1. 训练阶段：输入配对图像-文本的池化激活，通过JSAE优化上述复合损失，得到视觉和文本解码器及编码器。
  2. 特征发现阶段：在验证集上运行JSAE，对神经元（潜在维度）进行激活轮廓分析和聚类，选出代表特定语义概念的特征簇。
  3. 干预阶段：对目标层图像token位置添加或减去聚合的概念引导向量，观察生成文本的变化。

## 3. 实验设计：使用的数据集、基准、对比方法

- **数据集**：
  - **COCO-Probe**（25k对，来自10k图像）：用于层选择（线性探针）和干预测试（文中未明确说明是否用于特征发现，但JSAE训练使用50k正例对，来自COCO训练集）。
  - **JSAE训练**：50k正例图像-字幕对（COCO训练集）。
  - **探测与干预**：COCO验证集（不重叠）。
  - **跨数据集泛化**：Flickr30k（1500张图像，每类500张），测试类别转移鲁棒性。
- **基准（baseline）**：
  - **Prompt Steering**：用前置文本指令（如“描述食物”）衡量模型自然遵从性。
  - **Random Noise**：注入与引导向量范数匹配的高斯噪声。
  - **Activation Addition (ActAdd)**：基于对比激活差的优化无关方法。
  - **VL-SAE**：标准VLM SAE（共享字典，无显式对齐），在相同层（Layer 13）训练，重建和稀疏度匹配JSAE。
  - **JSAE w/o Alignment**：去掉对齐损失（\( \lambda_a = 0 \)）的消融。
- **评估指标**：
  - **Similarity Gain (\( \Delta_{sim} \))**：使用SBERT（all-MiniLM-L6-v2）计算引导后与目标描述余弦相似度的增加值。成功定义：\( \Delta_{sim} > 0.2 \)。
  - **Accuracy / Coherence**：由claude-sonnet-4在1-10分尺度上评分。
  - **Fluency**：PPL比率（\( r_{PPL} = PPL_{steer} / PPL_{base} \)）。
  - **Success Ratio**：成功干预（\( \Delta_{sim} > 0.2 \)）占测试样本比例。
- **统计验证**：使用Welch's t检验（Bonferroni校正）比较不同层的 \( \Delta_{sim} \) 分布。

## 4. 资源与算力

- **GPU型号**：NVIDIA A100 (80GB)。
- **训练时长**：
  - 每个JSAE训练约需6 A100-hours（20个epoch）。
  - 总预算约100 A100-hours（含所有SAE训练和干预实验）。
- **模型**：LLaVA-v1.6-Mistral-7B、Qwen3-VL-30B-Instruct（MoE）、Llama3-LLaVA-Next-8B。
- **文中明确指出了计算资源信息**。

## 5. 实验数量与充分性

- **实验数量**：
  - **层维度**：在LLaVA-7B上对4个层（7, 13, 25, 30）进行干预测试，包含多类别（食物、室内、冲浪、棒球、滑雪、网球、野生动物等），每层样本数28-112不等。
  - **模型维度**：3个不同架构（密集和MoE）各在其最优层进行评估。
  - **消融与对比**：5种baseline + 1种消融，均在Layer 13（探测最优层）比较。
  - **跨数据集**：Flickr30k 1500张图像，3类场景。
  - **统计检验**：层间及方法间的Welch's t检验。
- **充分性评估**：
  - **客观公平**：采用自动指标（SBERT相似度、PPL）和独立LLM评分（Claude），两者排名一致（Spearman ρ=1.0对层排序）。成功标准由自动指标定义，避免了人工偏差。基线超参数匹配（如VL-SAE重建与稀疏度相似）。
  - **充分性**：消融实验验证了对齐损失的必要性；跨架构泛化测试了MoE和密集模型；额外在Flickr30k上验证了泛化能力。整体实验设计较为系统，但局限于单层干预和高层概念。

## 6. 论文的主要结论与发现

- **层依赖不对称**：加法引导效果在中后层（Layer 25）达到峰值（Success Ratio 79.8%），在输出层（Layer 30）急剧下降（15.2%）；抑制效果在各层保持稳定（5.04-5.78分，统计上无显著差异）。
- **联合对齐至关重要**：去掉对齐损失的JSAE成功率仅4.1%，VL-SAE为0%，而完整JSAE在Layer 13达到74.0%。
- **优于现有方法**：JSAE显著优于ActAdd（39.4%）和Prompt Steering（22.1%），尤其是细粒度概念（如食物、野生动物）。
- **跨架构可推广**：JSAE在MoE模型Qwen3-VL-30B（Layer 33：77.9% Succ）和Llama3-LLaVA-8B（Layer 15：66.7% Succ）上均有效，表明该方法不局限于LLaVA。
- **探针准确度并非引导有效性的充分预测指标**：Layer 13探针最优，但Layer 25引导成功率最高。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 显式的代码级余弦对齐约束直接产生跨模态方向匹配的稀疏特征，而非仅依赖共现统计。
  - 设计理由几何分析（公式3-4）提供了理论直觉，并经验证。
- **实验设计**：
  - 使用自动指标结合独立LLM评判，避免单一评价来源偏差。
  - 严格的统计检验（Welch's t检验、Bonferroni校正）支撑层间差异结论。
  - 在三个不同架构上验证泛化性，并做了跨数据集（Flickr30k）测试。
  - 重建保真度验证（CE Loss Recovered）确保SAE不丢失关键信息。
- **可解释性**：通过聚类将原子特征组合为语义概念（如食物），使干预具有语义意义。

## 8. 不足与局限

- **粒度与空间控制**：JSAE使用全局平均池化，因此干预是全局的、均匀广播到所有图像token，无法进行精细的局部对象级编辑。未来工作需探索token级对齐和注意力引导。
- **特征聚合 vs 单语义性**：SAE本身存在特征分裂，需要通过后处理聚类得到语义概念；理想情况应是分层结构无后处理，更大字典或TopK SAE可能改善。
- **实验覆盖范围有限**：
  - 仅测试了单层干预，未探索跨层联合干预。
  - 概念仅限高层的MS-COCO类别（食物、运动等），未覆盖更复杂的几何推理或抽象概念。
  - 跨数据集（Flickr30k）测试显示类别特定退化（如“室内房间”成功率从0.675降至0.340），存在域敏感性。
  - 仅在一个MoE模型和一个Llama3上验证，仍需更广泛模型覆盖。
- **引导强度与幻觉风险**：强引导可导致与环境不符的描述（如输出示例中将社交聚会错生成为大象群），提示存在幻觉风险。
- **抑制效果的量化局限性**：抑制得分缺乏未干预或随机方向的参考基线，绝对分值的解释应谨慎。
- **计算成本**：每层每个模型需训练独立JSAE（6 GPU-hours），总预算100 A100-hours，对于更大规模或更多层的全面分析可能有扩展限制。

（完）
